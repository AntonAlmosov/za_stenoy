<%= react_component("templates/admin/OfflineIssueEditorTemplate", {issue: @issue, initialAuthors: @initialAuthors, initialPages: @initialPages, coverUrl: @cover, postPath: @post_path, backPath: @back_path, closePath: @close_path, origin: 'edit'}) %>